// import 'package:flutter/material.dart';

// class SH {
//   //init method is static so no object creation is required
//   static void init({
//     required context,
//   }) {
//     //instantiate variables here
//     _mediaQueryData = MediaQuery.of(context);
//     screenHeight = _mediaQueryData.size.height;
//   }

//   //declare variables here
//   static late MediaQueryData _mediaQueryData;
//   static late double screenHeight;

//   static double five = (5 / screenHeight) * screenHeight;
//   static double six = (6 / screenHeight) * screenHeight;
//   static double seven = (7 / screenHeight) * screenHeight;
//   static double ten = (10 / screenHeight) * screenHeight;
//   static double fifteen = (15 / screenHeight) * screenHeight;
//   static double seventeen = (17 / screenHeight) * screenHeight;
//   static double eighteen = (18 / screenHeight) * screenHeight;
//   static double twenty = (20 / screenHeight) * screenHeight;
//   static double twenty2 = (22 / screenHeight) * screenHeight;
//   static double twentyFive = (25 / screenHeight) * screenHeight;
//   static double thirty = (30 / screenHeight) * screenHeight;
//   static double thirty5 = (35 / screenHeight) * screenHeight;
//   static double fourty = (40 / screenHeight) * screenHeight;
//   static double fourty5 = (45 / screenHeight) * screenHeight;
//   static double fifty = (50 / screenHeight) * screenHeight;
//   static double fifty3 = (53 / screenHeight) * screenHeight;
//   static double fifty5 = (55 / screenHeight) * screenHeight;
//   static double sixty = (60 / screenHeight) * screenHeight;
//   static double sixty5 = (65 / screenHeight) * screenHeight;
//   static double seventy = (70 / screenHeight) * screenHeight;
//   static double eighty = (80 / screenHeight) * screenHeight;
//   static double hundred = (100 / screenHeight) * screenHeight;
//   static double one20 = (120 / screenHeight) * screenHeight;
//   static double one50 = (150 / screenHeight) * screenHeight;
//   static double one65 = (165 / screenHeight) * screenHeight;
// }

// class SW {
//   static void init({
//     required context,
//   }) {
//     //instantiate variables here
//     _mediaQueryData = MediaQuery.of(context);
//     screenWidth = _mediaQueryData.size.width;
//   }

//   //declare variables here
//   static late MediaQueryData _mediaQueryData;
//   static late double screenWidth;

//   static double seven = (7 / screenWidth) * screenWidth;
//   static double five = (5 / screenWidth) * screenWidth;
//   static double three = (3 / screenWidth) * screenWidth;
//   static double six = (6 / screenWidth) * screenWidth;
//   static double eight = (8 / screenWidth) * screenWidth;
//   static double ten = (10 / screenWidth) * screenWidth;
//   static double eleven = (11 / screenWidth) * screenWidth;
//   static double twelve = (12 / screenWidth) * screenWidth;
//   static double thirteen = (13 / screenWidth) * screenWidth;
//   static double fourteen = (14 / screenWidth) * screenWidth;
//   static double fifteen = (15 / screenWidth) * screenWidth;
//   static double sixteen = (16 / screenWidth) * screenWidth;
//   static double seventeen = (17 / screenWidth) * screenWidth;
//   static double eighteen = (18 / screenWidth) * screenWidth;
//   static double ninteen = (19 / screenWidth) * screenWidth;
//   static double twennty = (20 / screenWidth) * screenWidth;
//   static double twenty5 = (25 / screenWidth) * screenWidth;
//   static double thirty = (30 / screenWidth) * screenWidth;

//   static double thirty5 = (35 / screenWidth) * screenWidth;
//   static double fourty = (40 / screenWidth) * screenWidth;
//   static double fourty5 = (45 / screenWidth) * screenWidth;
//   static double fifty = (50 / screenWidth) * screenWidth;
//   static double sixty = (60 / screenWidth) * screenWidth;
//   static double sixty5 = (65 / screenWidth) * screenWidth;
//   static double eighty = (80 / screenWidth) * screenWidth;
//   static double hundred = (100 / screenWidth) * screenWidth;
//   static double one10 = (110 / screenWidth) * screenWidth;
//   static double one20 = (120 / screenWidth) * screenWidth;
//   static double two50 = (250 / screenWidth) * screenWidth;
//   static double three100 = (300 / screenWidth) * screenWidth;
// }
